name: Publish Release

permissions:
  contents: write

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 7.0.x

      - name: Get Version
        id: version
        uses: notiz-dev/github-action-json-property@release
        with:
          path: "Flow.Launcher.Plugin.TimeIn/plugin.json"
          prop_path: "Version"

      - run: echo ${{steps.version.outputs.prop}} 

      - name: Build
        run: |
          dotnet publish Flow.Launcher.Plugin.TimeIn -c Release -r win-x64 --no-self-contained
          Compress-Archive -Path Flow.Launcher.Plugin.TimeIn/bin/Release/win-x64/publish/* -DestinationPath "./Flow.Launcher.Plugin.TimeIn-v${{steps.version.outputs.prop}}.zip" -Force

      - name: Publish
        uses: softprops/action-gh-release@v1
        with:
          files: "Flow.Launcher.Plugin.TimeIn-v${{steps.version.outputs.prop}}.zip"
          tag_name: "v${{steps.version.outputs.prop}}"